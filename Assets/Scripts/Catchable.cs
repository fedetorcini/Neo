using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Catchable : Interactable
{
    /*public string mote;
    public bool isFed;

    private PlayerCombatScript player;

    void Start()
    {
        isFed = false;
    }
    public override int Interact(Transform interacting)
    {
        FindObjectOfType<InteractionMenuManager>().StartInteraction();
        player = interacting.GetComponent<PlayerCombatScript>();
        return 0;
    }

    public void Feed()
    {
        isFed = true;
        WorldReGen.LvlDown();
        GetComponent<MobAIScript>().SetMaster(player);
    }*/
}
